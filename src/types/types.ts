export interface inventario {
    id : number,
    nombre : string,
    tipo : string
}

export interface imagenes {
    id : number,
    link : string
}

export interface noticia {
    id: number,
    titulo: string,
    texto: string,
    link: string
}